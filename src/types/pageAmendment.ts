/**
 * Options for page amendemnts (header and footer)
 */
export type PageAmendmentOptions = {
    /**
     * Whether the header should be enabled
     * @default true
     */
    enableHeader?: boolean;

    /**
     * Whether the footer should be enabled
     * @default true
     */
    enableFooter?: boolean;
};
